[package]
name = "yal-ipc"
version = "0.0.0"
edition = "2024"

[dependencies]
futures = "0.3.31"
serde_json = "1.0.145"
tarpc = { version = "0.37.0", features = ["serde-transport", "serde-transport-json", "unix"] }
tokio = { version = "1.48.0", features = ["fs", "net"] }
tokio-serde = "0.9.0"
yal-core = { path = "../yal-core" }
